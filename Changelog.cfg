
// Changelog.cfg v1.1.1.0
// QuickBASIC's ISRUThanksButNoTanks (QBTT)
// created: 2020 02 25
// updated: 06 Sept 2021

KERBALCHANGELOG
{
	showChangelog = True
	modName = QuickBASIC's ISRU Thanks But No Tanks
	license = Expat-MIT
	author = QuickBASIC, zer0Kerbal
	VERSION
	{
		version = 1.0.0.0
		versionName = Brushing off the Construction Dust
		versionDate = tbd
		versionKSP = 1.12.2
		change = fit and polish for final release
		change = update HeroLogo1920x1920.png
	}
	VERSION
	{
		version = 0.1.4.2
		versionName = Thank you QuickBASIC 
		versionDate = 2021-09-06
		versionKSP = 1.12.2
		change = >>-- adopted for curation by @zer0Kerbal --<<
		change = <b style="color:red">DO A CLEAN INSTALL: DELETE EXISTING THEN RE-INSTALL</b>
		CHANGE
		{
			type = Update
			change = ISRUThanksButNoTanks.cfg:
			subChange = modernization, polish, update pass on .cfg
			subChange = added `:NEEDS` and `:FOR` where appropriate
			subChange = added header and footer
		} 
		CHANGE
		{
			type = Behind the scenes
			change = organize for adoption
			subChange = folder structure
			subChange = automated deploy/release process
			subChange = automated backend
			subChange = _Release.json and jsons
			subChange = moved changelog into separate file
		}
		CHANGE
		{
			type = Create
			change = Changelog, .version, Readme.md
			subChange = added/updated license(s) file(s)
			subChange = added/updated .version file
			subChange = added/updated Readme
			subChange = created Changelog.cfg [KERBALCHANGELOG] (.this)
			subChange = added additional fields in .version (might need to tweak urls)
			subChange = added shields to Readme.md
			subChange = create HeroLogo1920x1920.png
		}
		CHANGE
		{
			type = Created
			change = Online
			subChange = GitHub Repo
			subChange = created Forum Thread
			subChange = updated SpaceDock
			subChange = updated CKAN/NetKAN
		}
		CHANGE
		{
			change = Adoption by zer0Kerbal
			subChange = Send Adoption Papers
			subChange = Post Adoption Notice
			subChange = gather/store legalmumbojumbo
			subChange = verify licenses
			subChange = look for existing repo/postings
		}
	}
	VERSION
	{
		version = 0.1.4.1
		versionName = the pre-adoption party
		change = Beta Release 09/09/2014
		change = forked from QuickBASIC's github repo
	}
}

// GPLv2
// zer0Kerbal